{
  "name": "portfolio-backend",
  "version": "1.0.0",
  "description": "Backend deployment helpers for portfolio AWS Lambda API",
  "main": "lambda/index.js",
  "scripts": {
    "lambda:install": "cd lambda && npm ci --omit=dev",
    "lambda:package": "cd lambda && zip -r ../../function.zip index.js package.json node_modules"
  },
  "keywords": [
    "lambda",
    "aws",
    "api",
    "chatbot"
  ],
  "author": "Oussema Amri",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
